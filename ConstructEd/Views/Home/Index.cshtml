@{
    ViewData["Title"] = "Home Page";
}

@* <style>
    /* Cube-specific styles */
    .cube-section {
        margin-top: 150px;
        display: flex;
        justify-content: center;
        align-items: center;
        padding-bottom: 200px;
    }

    .cube-container {
        width: 200px;
        height: 200px;
        perspective: 800px;
        margin: auto;
        transition: 0.8s ease-out;
    }

        .cube-container:hover {
            transform: scale(1.5);
        }

    .cube {
        position: relative;
        width: 100%;
        height: 100%;
        transform-style: preserve-3d;
        animation: rotate 8s infinite linear;
    }

    .face {
        position: absolute;
        width: 200px;
        height: 200px;
        color: #00619e; /* Cream color for text */
        font-size: 18px;
        text-align: center;
        line-height: 200px;
        background: rgb(100,100,100,0.05); /* Main color for cube faces */
        border: 2px solid #01a199; /* Secondary color for borders */
    }

    .front {
        transform: translateZ(100px);
    }

    .back {
        transform: rotateY(180deg) translateZ(100px);
    }

    .right {
        transform: rotateY(90deg) translateZ(100px);
    }

    .left {
        transform: rotateY(-90deg) translateZ(100px);
    }

    .top {
        transform: rotateX(90deg) translateZ(100px);
    }

    .bottom {
        transform: rotateX(-90deg) translateZ(100px);
    }

    @@keyframes rotate {
        0% {
            transform: rotateX(0) rotateY(0) rotateZ(0);
        }

        100% {
            transform: rotateX(360deg) rotateY(360deg) rotateZ(360deg);
        }
    }
</style> *@

<!-- 3D Cube Section -->
<section>
    <div class="cube-section">
        <div class="cube-container">
            <div class="cube">
                <div class="face front"><span>Python</span></div>
                <div class="face back">Dynamo</div>
                <div class="face right">Revit API</div>
                <div class="face left">C#</div>
                <div class="face top">WPF</div>
                <div class="face bottom">Civil 3D</div>
            </div>
        </div>
    </div>
</section>